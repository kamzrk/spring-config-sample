# Config Server => localhost:8081
spring.cloud.config.uri=http://localhost:8081
spring.application.name=bootiful

# Vault Server => localhost:8200
spring.cloud.vault.scheme=http
spring.cloud.vault.token=${VAULT_TOKEN}
